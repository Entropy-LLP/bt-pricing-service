services:
  - type: web
    name: bt-pricing-service
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: "3003"
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
